VOL_DIR=${HOME}/data

USER_NAME={{ user_name }}

DB_NAME={{ db_name }}
DB_HOST=mariadb:3306

WP_ROOT=/var/www/html
WP_URL=https://{{ ansible_host | default(inventory_hostname) }}
WP_TITLE=cloud-1
WP_EMAIL={{ wp_email }}
WP_LOCALE=en_US
WP_THEME=twentytwentyone

IP_ADDR={{ ansible_host | default(inventory_hostname) }}
